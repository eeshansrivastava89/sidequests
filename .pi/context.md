# Session Context
<!-- Auto-generated by context-save -->
<!-- Last saved: 2026-04-07 07:36 local -->

## Project Anchor
- Project: sidequests (local dev project tracker)
- Goal: Enable multiple LLM providers (OpenRouter, Ollama, Qwen CLI, Codex CLI)
- Current phase: Completed - v0.3.10 published

## Git Snapshot
- Branch: main
- Recent commits:
  - fdaf8b8 0.3.10
  - 358f442 feat: enable openrouter, ollama, qwen-cli providers
  - eee653b 0.3.9
  - f024a0a fix: activity log sort, scan animation, lint config, and default claude model
- Working tree changes: none

## What We Were Doing
- Task: Issue #2 - Enable LLM providers in settings UI
- Progress:
  - [x] Add Qwen CLI provider with headless mode (JSON output, auto-approve)
  - [x] Enable OpenRouter and Ollama in provider dropdown
  - [x] Remove MLX from UI (per user request)
  - [x] Update model defaults (Sonnet 4.6, GPT-5.4)
  - [x] Simplify Codex model to just GPT-5.4
  - [x] Commit and push to GitHub
  - [x] Publish v0.3.10 to npm
- Stop point: Published v0.3.10, issue #2 already closed (from earlier)

## Decisions
- Qwen CLI uses built-in coder model (no model selection needed)
- Codex CLI simplified to GPT-5.4 only (other models don't support vision)
- MLX removed per user request

## Open Items
- Remaining issues: #8 (project name collisions), #5 (LLM concurrency), #12 (LOC metric), #11 (selective scan), #10 (Next.js migration)

## Resume Plan
1. Pick next issue to work on (suggest #8 - project name collisions)
2. Or test the new providers locally with `npm run dev`
3. Or review remaining open issues

## Notes
- Qwen CLI uses headless mode: `qwen -p <prompt> --output-format json --approval-mode auto_edit`
- Claude default: claude-sonnet-4-6
- Codex default: gpt-5.4 (supports vision)
- Qwen has no model selection (uses built-in qwen3-coder-plus)